# API Security Dojo - Java Spring Boot Configuration
server.port=3004

# CORS - VULNERABILITY V08: Allow all origins with credentials
# spring.mvc.cors.allowed-origins=*

# SQLite Database
spring.datasource.url=jdbc:sqlite:dojo.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Documentation mode
dojo.mode=${DOJO_MODE:challenge}
